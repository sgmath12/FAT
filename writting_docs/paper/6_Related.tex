\section{Related Work}
\label{sec:related}

% STRUCTURE TO BE SET BY THE AUTHORS.  Placeholder so \Cref{sec:related} resolves and so the
% introduction can defer the ARREST / DP-FAT / B-MTARD detail here rather than carrying it.
%
% Intended coverage, from the introduction's forward reference:
%   - adversarial training and the accuracy-robustness trade-off
%   - target softening inside AT: label smoothing, consistency regularization, ADR
%   - adversarial distillation from a robust teacher: ARD, IAD, RSLAD, AKD, AdaAD, IGDM
%   - methods guided by a naturally trained network: LBGAT, ARREST, DP-FAT, B-MTARD  <- closest to ours
%   - per-sample attack radii: IAAT, MMA, CAT
%
% The distinction this section has to make, and which the introduction only states in one sentence:
% in every prior use of a natural network the logits are the transferred quantity and are balanced
% against a label term or a second teacher, so a coefficient has to be chosen.  Ours replaces the
% label term with a feature anchor, which is what removes the coefficient and what makes
% Proposition 1 available.

\emph{To be written.}
