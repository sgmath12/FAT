\section{Experiments}
\label{sec:exp}

\subsection{Experimental Settings}

\paragraph{Setup.} CIFAR-10, CIFAR-100 \citep{krizhevsky2009learning} and Tiny-ImageNet-200
\citep{le2015tiny}, with random crop and horizontal flip only. For each dataset the teacher is a
network of the same architecture trained naturally for 200 epochs on the same data
(\Cref{tab:teachers}); no adversarial training enters its construction. The student is initialized
from it and trained for 100 epochs with AdamW at learning rate $0.021$ under a one-cycle schedule,
batch size $128$, with a 10-step PGD attack at step size $2/255$ and training radius $8.8/255$
(\Cref{sec:radius} varies it). Weight averaging ($\kappa = 0.999$, from $20\%$ of the run) and an AWP
proxy \citep{2020_awp} ($\gamma = 0.005$, after epoch $10$) are used and ablated in
\Cref{sec:components}. Every dataset uses this identical configuration; only the teacher checkpoint
changes. ResNet-18 throughout except \Cref{tab:wrn}.

\paragraph{Baselines and evaluation.} Every baseline in \Cref{tab:main} is run here rather than
quoted: PGD-AT, TRADES and MART as standard adversarial training; ARD, RSLAD, AdaAD and IGDM as
adversarial distillation, given our natural teacher in place of the robust one they assume; and
RPAT++ reproduced from its own repository. Each row states in its \emph{Teacher} column what it
assumes available, cells we could not run are left empty with the reason, and published figures for
all of them are collected in \Cref{tab:published}. We report clean accuracy and AutoAttack (standard
version) on the full test set at $\epsilon = 8/255$, with $\mathrm{Avg}$ their mean and NRR their
harmonic mean; per-attack values are in \Cref{tab:fullmetrics}. All of our numbers are the final-epoch
weight-averaged model, with no best-checkpoint selection.

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{4.5pt}
  \caption{Teacher models. AutoAttack accuracy is $0$ by construction: these networks never see an
  adversarial example.}
  \label{tab:teachers}
  \begin{tabular}{llrrr}
    \toprule
    Dataset & Teacher & Epochs & Clean & AA \\
    \midrule
    CIFAR-10      & ResNet-18, natural & 200 & 95.32 & 0.00 \\
    CIFAR-100     & ResNet-18, natural & 200 & 77.66 & 0.00 \\
    Tiny-ImageNet & ResNet-18, natural & 200 & 66.29 & 0.00 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Main Results}

Baselines are grouped by what they assume is available: \textbf{(a)} standard adversarial
training; \textbf{(b)} adversarial distillation from an \emph{adversarially trained} teacher;
\textbf{(c)} methods guided by a \emph{naturally} trained network, the family ours belongs to; and
\textbf{(d)} current accuracy--robustness trade-off methods. Published numbers carry their source;
`--' means the paper does not report that cell for this dataset and architecture.

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{2.6pt}
  \caption{Main results. ResNet-18, $\ell_\infty$, $\epsilon = 8/255$. \textbf{Every number is measured
  in this framework}---one data pipeline, one attack implementation, one evaluation, the final-epoch
  weight-averaged model---so the rows are comparable to each other rather than across papers, and no
  source column is needed. \textbf{Teacher} is what the method assumes available: \emph{--} none,
  \emph{self} its own weight average, \emph{nat.} a naturally trained network. Rows we have not run are
  left empty with the reason. $\mathrm{Avg} = \tfrac12(\text{Clean}+\text{AA})$, the criterion B-MTARD
  calls W-Robust; NRR is the harmonic mean. \textsuperscript{j}~LBGAT trains its
  natural branch jointly, so its teacher is not free. \textsuperscript{l}~HAT uses a natural network
  only to \emph{label} helper points beyond the ball, never as a target, so it is grouped with the
  trade-off methods. \ddg~IGDM is reported at $\alpha = 1$; its published
  $\alpha = 20$ diverges to chance accuracy on both datasets when the teacher is natural, for the
  reason given in \Cref{sec:natteacher}. Published figures for these methods are in
  \Cref{tab:published}, Tiny-ImageNet in \Cref{tab:tin}, WideResNet in \Cref{tab:wrn}, and
  per-attack metrics in \Cref{tab:fullmetrics}.}
  \label{tab:main}
  \begin{tabular}{llrrrrrrrrl}
    \toprule
    & & \multicolumn{4}{c}{CIFAR-10} & \multicolumn{4}{c}{CIFAR-100} & \\
    \cmidrule(lr){3-6}\cmidrule(lr){7-10}
    Method & T. & Clean & AA & Avg & NRR & Clean & AA & Avg & NRR & \\
    \midrule
    PGD-AT                           & --    & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    TRADES                           & --    & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    MART                             & --    & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    PGD-AT, init.\ at the teacher    & nat.  & 82.23 & 50.72 & 66.47 & 62.74 & 57.73 & 26.46 & 42.09 & 36.29 &  \\
    \midrule
    ARD                              & nat.  & 84.58 & 43.75 & 64.16 & 57.67 & 57.61 & 20.24 & 38.92 & 29.96 &  \\
    RSLAD                            & nat.  & 85.15 & 42.91 & 64.03 & 57.06 & 59.68 & 21.30 & 40.49 & 31.40 &  \\
    AdaAD                            & nat.  & 85.39 & 46.22 & 65.81 & 59.98 & 59.79 & 23.19 & 41.49 & 33.42 &  \\
    AdaAD + IGDM\ddg                 & nat.  & 85.01 & 45.50 & 65.25 & 59.27 & 48.25 & 19.48 & 33.87 & 27.75 &  \\
    LBGAT                            & nat.\textsuperscript{j} & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    \midrule
    HAT                              & nat.\textsuperscript{l} & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    ADR                              & self  & -- & -- & -- & -- & -- & -- & -- & -- & \emph{running} \\
    CURE                             & self  & -- & -- & -- & -- & -- & -- & -- & -- & \emph{does not reproduce} \\
    RPAT++                           & --    & 82.41 & 50.75 & 66.58 & 62.82 & 55.93 & 27.36 & 41.64 & 36.74 &  \\
    \midrule
    \textbf{CFA (ours)}\dg           & nat.  & \textbf{84.96} & \textbf{51.74} & \textbf{68.35} & \textbf{64.31} & \textbf{62.17} & \textbf{28.86} & \textbf{45.52} & \textbf{39.42} &  \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{4.5pt}
  \caption{Tiny-ImageNet-200, ResNet-18. Same eleven rows and the same protocol as \Cref{tab:main};
  \textbf{--} marks a cell we have not run. Our row uses the 200-epoch teacher; the 80-epoch teacher
  gives $57.08 / 18.96$, which is the teacher-length dial of \Cref{tab:teacherladder} on a third
  dataset. ADR reports $48.55 / 20.23$ here (\Cref{tab:published}).}
  \label{tab:tin}
  \begin{tabular}{llrrrr}
    \toprule
    Method & Teacher & Clean & AA & Avg & NRR \\
    \midrule
    PGD-AT                           & --    & -- & -- & -- & -- \\
    TRADES                           & --    & -- & -- & -- & -- \\
    MART                             & --    & -- & -- & -- & -- \\
    PGD-AT, init.\ at the teacher    & nat.  & -- & -- & -- & -- \\
    \midrule
    ARD                              & nat.  & -- & -- & -- & -- \\
    RSLAD                            & nat.  & -- & -- & -- & -- \\
    AdaAD                            & nat.  & -- & -- & -- & -- \\
    AdaAD + IGDM                     & nat.  & -- & -- & -- & -- \\
    \midrule
    LBGAT                            & nat.\textsuperscript{j} & -- & -- & -- & -- \\
    \midrule
    HAT                              & nat.\textsuperscript{l} & -- & -- & -- & -- \\
    ADR                              & self  & -- & -- & -- & -- \\
    CURE                             & self  & -- & -- & -- & -- \\
    RPAT++                           & --    & -- & -- & -- & -- \\
    \midrule
    \textbf{CFA (ours)}              & nat.  & \textbf{55.16} & \textbf{20.54} & 37.85 & \textbf{29.93} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{4.5pt}
  \caption{WideResNet-34-10, $\ell_\infty$, $\epsilon = 8/255$. Same eleven rows and the same protocol
  as \Cref{tab:main}; \textbf{--} marks a cell we have not run. The comparison line on CIFAR-100 is
  ADR's WideResNet-34-10 result, $62.21$ clean / $31.60$ AA (\Cref{tab:published}).}
  \label{tab:wrn}
  \begin{tabular}{llrrrrrr}
    \toprule
    & & \multicolumn{3}{c}{CIFAR-10} & \multicolumn{3}{c}{CIFAR-100} \\
    \cmidrule(lr){3-5}\cmidrule(lr){6-8}
    Method & T. & Clean & AA & NRR & Clean & AA & NRR \\
    \midrule
    PGD-AT                           & --    & -- & -- & -- & -- & -- & -- \\
    TRADES                           & --    & -- & -- & -- & -- & -- & -- \\
    MART                             & --    & -- & -- & -- & -- & -- & -- \\
    PGD-AT, init.\ at the teacher    & nat.  & -- & -- & -- & -- & -- & -- \\
    \midrule
    ARD                              & nat.  & -- & -- & -- & -- & -- & -- \\
    RSLAD                            & nat.  & -- & -- & -- & -- & -- & -- \\
    AdaAD                            & nat.  & -- & -- & -- & -- & -- & -- \\
    AdaAD + IGDM                     & nat.  & -- & -- & -- & -- & -- & -- \\
    \midrule
    LBGAT                            & nat.\textsuperscript{j} & -- & -- & -- & -- & -- & -- \\
    \midrule
    HAT                              & nat.\textsuperscript{l} & -- & -- & -- & -- & -- & -- \\
    ADR                              & self  & -- & -- & -- & -- & -- & -- \\
    CURE                             & self  & -- & -- & -- & -- & -- & -- \\
    RPAT++                           & --    & -- & -- & -- & -- & -- & -- \\
    \midrule
    \textbf{CFA (ours)}              & nat.  & \multicolumn{3}{c}{\emph{in progress}} & \multicolumn{3}{c}{\emph{in progress}} \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Reproductions, and one failure.} RPAT++ we reproduce to within $0.31$ AutoAttack of its
published numbers on both datasets and report our own measurement. CURE we could \emph{not}: seven runs
of the official repository fall short on \emph{different} axes, and \Cref{app:cure} gives the detail.
Its row is therefore left empty rather than filled from the paper. Published figures for every method
here, including the ones we did not run, are collected in \Cref{tab:published}.

\subsection{Published Distillation Objectives, Given a Natural Teacher}
\label{sec:natteacher}

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{4.5pt}
  \caption{The four published distillation objectives given the teacher they would have in our
  setting---our naturally trained network in place of the adversarially trained one they assume. Every
  number is measured here: ResNet-18, 100 epochs, each method at its own published recipe, same data
  pipeline and evaluation as every other row we report. The reference rows use that same teacher for
  initialization only, or as an anchor. Blank cells are runs still in progress.}
  \label{tab:natteacher}
  \begin{tabular}{lrrrcrrrl}
    \toprule
    & \multicolumn{3}{c}{CIFAR-100} & & \multicolumn{3}{c}{CIFAR-10} & \\
    \cmidrule(lr){2-4}\cmidrule(lr){6-8}
    & Clean & AA & NRR & & Clean & AA & NRR & \\
    \midrule
    ARD                                  & 57.61 & 20.24 & 29.96 & & 84.58 & 43.75 & 57.67 &  \\
    RSLAD                                & 59.68 & 21.30 & 31.40 & & 85.15 & 42.91 & 57.06 &  \\
    AdaAD                                & 59.79 & 23.19 & 33.42 & & 85.39 & 46.22 & 59.98 &  \\
    AdaAD + IGDM\ddg                     & 48.25 & 19.48 & 27.75 & & 85.01 & 45.50 & 59.27 &  \\
    \midrule
    PGD-AT, initialized at the teacher   & 57.73 & \textbf{26.46} & 36.29 & & 82.23 & \textbf{50.72} & 62.74 &  \\
    CFA (ours), no WA/AWP                & 61.21 & 25.24 & 35.74 & & -- & -- & -- &  \\
    \bottomrule
  \end{tabular}
\end{table}

Every one of them lands below the row that does not distil at all: on both datasets, plain adversarial
training initialized from the same teacher beats all four. The accuracy a natural teacher holds is not
recovered by treating it as a logit target.

The IGDM row is reported at $\alpha = 1$ because its published $\alpha = 20$ does not train at all
here: it collapses to chance on both datasets ($1.00$ on CIFAR-100, $10.00$ on CIFAR-10), so ten
classes did not rescue it. This is not a porting artifact. With a natural teacher the IGDM target
$\mathrm{softmax}(f_t(x{+}\delta) - f_t(x{-}\delta))$ places $0.917$ of its mass on a single class and
the term reaches $17.3\times$ the AdaAD loss. At $\alpha = 1$, which puts the two terms on comparable
scales and is the most favourable reading available, it trains normally---and still finishes below
AdaAD alone on both datasets, by $11.54$ clean and $3.71$ AA on CIFAR-100 and by $0.38$ and $0.72$ on
CIFAR-10. \Cref{tab:linearity} gives the structural reason: IGDM's finite
difference is a gradient only while the teacher is locally linear, and a naturally trained teacher is
forty times further from linear than the ones it was designed for.

\subsection{Component Ablation}
\label{sec:components}

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{4.5pt}
  \caption{Component ablation. CIFAR-100, ResNet-18, raw-$\ell_2$ anchor with
  $\epsilon_{\mathrm{train}} = 8.8/255$ held fixed and no learning-rate freeze. Each row adds one
  component to the row above. Columns match \Cref{tab:main}; per-attack values are in
  \Cref{tab:fullmetrics}.}
  \label{tab:ladder}
  \begin{tabular}{lrrrr}
    \toprule
    & Clean & AA & Avg & NRR \\
    \midrule
    \multicolumn{5}{l}{\emph{50 epochs}} \\
    Anchor ($p=0$)                         & 61.33 & 26.19 & 43.76 & 36.71 \\
    \quad + sensitivity-matched $\epsilon$ & 62.94 & 26.40 & 44.67 & 37.20 \\
    \quad + weight averaging               & 61.11 & \textbf{28.06} & 44.59 & \textbf{38.46} \\
    \quad + AWP\dg                         & 59.90 & 28.05 & 43.98 & 38.21 \\
    \midrule
    \multicolumn{5}{l}{\emph{100 epochs}} \\
    Anchor ($p=0$)                         & 61.21 & 25.24 & 43.23 & 35.74 \\
    \quad + sensitivity-matched $\epsilon$ & 62.98 & 25.43 & 44.20 & 36.23 \\
    \quad + weight averaging               & 62.43 & 27.39 & 44.91 & 38.08 \\
    \quad + AWP\dg                         & 62.35 & \textbf{28.68} & 45.52 & \textbf{39.29} \\
    \bottomrule
  \end{tabular}
\end{table}

The sensitivity rule contributes exactly $+0.49$ NRR at both schedule lengths ($+1.61/{+}0.21$ at 50
epochs, $+1.77/{+}0.19$ at 100) at identical total attack budget. AWP changes sign with schedule
length: $-0.25$ NRR at 50 epochs, $+1.21$ at 100. And 50 epochs with weight averaging alone already
exceeds ADR's full stack ($38.46$ against $38.08$), at half the schedule and one fewer component, so
the gain is not the stack.

Leaving the teacher's classifier alone beats every alternative, including the best temperature. At the
full recipe the same holds---freezing the head scores $62.65/28.77/$NRR $39.43$ against
$62.35/28.68/39.29$ with the head-KD term retained---and this is what removes $\tau$ and $\beta$ from
the method. Both arms of that comparison predate the AWP correction described in \Cref{app:awp} and
are quoted as measured, so the pair is internally consistent; the corrected number for the frozen-head
arm is the $62.17/28.86/39.42$ reported everywhere else.

\begin{table}[t]
  \centering \small \setlength{\tabcolsep}{5pt}
  \caption{Two controls on the anchor itself. CIFAR-100, ResNet-18, base regime---teacher warm start,
  no weight averaging, no AWP, $p = 0$, $\epsilon_{\mathrm{train}} = 8.8/255$---so that no stack
  component can absorb the difference. Each row differs from its reference in exactly one line of
  configuration.}
  \label{tab:controls}
  \begin{tabular}{lrrrrr}
    \toprule
    & Clean & PGD-20 & CW & AA & NRR \\
    \midrule
    \multicolumn{6}{l}{\emph{(a) where the teacher is read} (50 epochs)} \\
    Anchor, $\Phi_t$ read at $x$              & 61.33 & 32.94 & 27.80 & \textbf{26.19} & \textbf{36.71} \\
    \quad read at $x_{\mathrm{adv}}$ instead  & \textbf{76.11} & 0.00 & 0.00 & \textbf{0.00} & 0.00 \\
    \midrule
    \multicolumn{6}{l}{\emph{(b) the anchor against label CE, same regime} (100 epochs)} \\
    Anchor                                    & \textbf{61.21} & \textbf{31.26} & \textbf{26.82} & \textbf{25.24} & \textbf{35.74} \\
    \quad label cross-entropy instead         & 54.60 & 20.60 & 21.21 & 19.45 & 28.68 \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Where the anchor's robustness comes from.} \Cref{tab:controls}(a) moves $\Phi_t$ from $x$
to $x_{\mathrm{adv}}$ in both the loss and the attack, and changes nothing else. Robustness is not
degraded but eliminated: $0.00$ under PGD-20, CW and AutoAttack alike, while clean accuracy
\emph{rises} $14.78$ points to within $1.55$ of the natural teacher. The reason is visible in the
objective's value, which falls to $6.26$ against roughly $50.7$ for the anchor. Once $\Phi_t$ is
evaluated inside the ball, $\Phi_s = \Phi_t$ minimizes \Cref{eqn:anchor} exactly---and that solution
is a natural model. Reading the teacher at the clean point is not a modelling preference; it is the
only thing standing between this objective and a degenerate one.

\paragraph{What the anchor is worth on its own.} The comparison against cross-entropy in
\Cref{tab:main} is not matched in stack: our row carries weight averaging and AWP and the
cross-entropy row carries them too, but the \emph{anchor} has never been asked what it does with
neither. \Cref{tab:controls}(b) asks. At the anchor's own base regime---same teacher initialization,
same schedule, same training radius, no sensitivity-matched $\epsilon$, no weight averaging, no
AWP---replacing the anchor with label cross-entropy costs $6.61$ clean \emph{and} $5.79$ AutoAttack.
The anchor is therefore not merely compatible with robustness, as \Cref{prop:decomp} alone would
license; it is a source of it, and at matched stack the larger source. For scale, the shipped recipe
leads cross-entropy \emph{with} weight averaging and AWP by $2.40$ AutoAttack.

One objection deserves its own measurement. Cross-entropy without weight averaging robustly overfits
at this schedule, so part of the $5.79$ could be decay rather than deficit. It does overfit---its
PGD-20 peaks at $23.31$ near epoch $70$ and decays to $21.13$---but the anchor does not: its PGD-20
rises monotonically to the final evaluation, $25.23 \to 28.86 \to 30.72 \to 31.47$. Measured against
cross-entropy's \emph{best} epoch rather than its last, the anchor's \emph{final} still leads by
$8.16$. Not robustly overfitting is itself a property of the objective here, and it is the property
weight averaging is normally bought to supply.

\subsection{Training Radius, Teacher Length, and Larger Architectures}
\label{sec:radius}
\label{sec:geometry}
\label{sec:wrn}

\paragraph{The training radius is a plateau, not a tuned point.} Sweeping
$\epsilon_{\mathrm{train}} \in \{8, 8.8, 10\}/255$ with everything else fixed and evaluating at
$8/255$ throughout (\Cref{tab:radius}), NRR peaks at $8.8/255$ on both datasets but the curve is mild,
and the standard $8/255$ already exceeds the best published result on either ($38.72$ against ADR's
$38.08$; $64.48$ against CURE's $63.19$). Past $8.8/255$ the radius buys nothing: AutoAttack and CW at
$10/255$ are identical to the last decimal to those at $8.8/255$ on both datasets, while standard
accuracy falls $2.66$ and $2.29$---the $\epsilon$-independence \Cref{app:robustness} predicts above the
threshold.

\paragraph{What the teacher transfers is geometry, not accuracy.} Changing only the teacher checkpoint
and leaving the student configuration untouched (\Cref{tab:teacherladder}), teacher accuracy and
student accuracy are anti-correlated at $r = -0.999$ across five teachers trained for 50 to 300
epochs: the teacher gains $2.51$ points and the student \emph{loses} $2.04$. NRR peaks in the middle,
so the teacher's training length is a control on the operating point, and locating it costs five
natural-training runs and no adversarial ones. The effect reproduces on Tiny-ImageNet where teacher
accuracy is controlled to $0.32$ points, so accuracy cannot be the cause; what varies monotonically is
the teacher's class separation.

\paragraph{Larger architectures.} WideResNet-34-10 at the same recipe on both CIFAR datasets is
reported in \Cref{tab:wrn}, alongside the published WideResNet rows.

