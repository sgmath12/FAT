% Abstract for the accuracy-robustness trade-off paper.
% Self-contained: compiles on its own with pdflatex, and the abstract block can be lifted
% verbatim into an ICLR/ICML/NeurIPS style file.
%
% Framing note, do not drop when transplanting: robust-teacher adversarial distillation
% (ARD / RSLAD / AdaAD / IGDM / B-MTARD) is a DIFFERENT problem, not a stronger result on ours.
% Those methods assume an already-robust WideResNet exists and ask how well it transfers into a
% small student; their robustness originates in the teacher.  IGDM+AdaAD accordingly reports
% 64.44 clean / 30.32 AA on ResNet-18 / CIFAR-100 from a BDM-AT WRN-28-10 teacher (72.58 / 38.83).
% Ours is the teacherless setting: robustness comes from the student's own inner maximization and
% the teacher contributes none of it -- it is never evaluated under attack, so it could not.
% Never write "we beat" against that line, and never write "a robust teacher is unnecessary".
% Within our own setting the strongest published AA is ADR+WA+AWP at 57.36 / 28.50.

\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage[T1]{fontenc}
\usepackage{microtype}

\title{Anchor, Don't Imitate:\\ Self-Distilled Feature Anchoring for the Accuracy--Robustness Trade-off}
\author{}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Adversarial training buys robustness by giving up clean accuracy, and it does so with no teacher at
all: the robustness comes from the inner maximization, and the clean accuracy is simply lost.
Adversarial distillation recovers some of it, but by importing an already-robust network---typically
a large WideResNet adversarially trained with extra data---so the robustness originates outside the
student and the teacher costs more than the student does. We keep the teacherless setting and ask a
narrower question: can the \emph{lost clean accuracy} be recovered from a copy of the student's own
architecture, trained naturally on the same data at no adversarial cost?
Such a teacher is the most accurate one available, but its accuracy rests on features an adversary
can flip, and copying it should import that fragility. We show that it does not, provided the teacher
is used as a \textbf{feature anchor} rather than a logit target. Our objective matches the teacher's
\emph{clean} feature at the \emph{adversarial} input, so the teacher is never evaluated under attack
and can transfer neither its instability nor, by the same token, any robustness. We prove that this
single term is equivalent, within a constant factor, to fidelity plus local stability, and we
allocate the per-sample attack radius by the loss's own input sensitivity at a fixed total budget.
Robustness therefore remains entirely the student's while the clean accuracy is recovered: we improve
clean accuracy by \textbf{4.8 points at matched AutoAttack} on CIFAR-100 and set a new
accuracy--robustness frontier on CIFAR-10, CIFAR-100 and Tiny-ImageNet. A naturally-trained teacher
further exposes a trade-off dial unavailable to robust-teacher distillation---teacher training
length---which moves the student along the frontier at no additional cost.
\end{abstract}

\section*{Short abstract (page-limited venues)}

\begin{quote}
Adversarial training gives up clean accuracy and recovers none of it: robustness comes from the inner
maximization and no teacher is involved. Adversarial distillation recovers some, but by importing an
already-robust network, so the robustness originates outside the student. We keep the teacherless
setting and use a naturally-trained copy of the student's own architecture---free---to recover only
the lost clean accuracy. The key is to consume it as a \textbf{feature anchor} rather than a logit
target: matching the teacher's \emph{clean} feature at the \emph{adversarial} input means the teacher
is never evaluated under attack, so it can transfer neither its fragility nor any robustness. We
prove this objective is equivalent within a constant factor to fidelity plus local stability, and
pair it with a per-sample attack radius set by the loss's own input sensitivity at a fixed total
budget. Robustness stays the student's own; clean accuracy improves by 4.8 points at matched
AutoAttack on CIFAR-100, with a new frontier on three datasets.
\end{quote}

\section*{Headline numbers}

\begin{center}
\begin{tabular}{lrrr}
\hline
 & clean & AA & NRR \\
\hline
CIFAR-100, ours & 62.17 & 28.59 & 39.17 \\
CIFAR-100, ADR + WA + AWP & 57.36 & 28.50 & 38.08 \\
CIFAR-10, ours & 84.66 & 51.87 & 64.33 \\
CIFAR-10, ADR + WA + AWP & 83.26 & 51.18 & 63.39 \\
Tiny-ImageNet, ours & 55.16 & 20.54 & 29.93 \\
Tiny-ImageNet, ADR + WA + AWP & 48.27 & 20.10 & 28.38 \\
\hline
\end{tabular}
\end{center}

\noindent
All ResNet-18, $\ell_\infty$, $\varepsilon=8/255$; AA is AutoAttack (\texttt{apgd-ce}+\texttt{apgd-t})
on the full test set; NRR is the harmonic mean of clean accuracy and AA.

\end{document}
