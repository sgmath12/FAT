# 2026-08-25 — Toy model 작업 메모 (clean-accuracy gain 설명 시도)

## 왜 이 toy example를 만들었나

`theory_v1.md` T.2b (Proposition 2, $L\le F+O\le3L$)는 "non-robust(자연) teacher를 앵커로 써도
robustness가 안 깎이는 이유"는 설명하지만, 논문이 실제로 파는 결과인 **clean accuracy 상승**
(예: ResNet18/CIFAR100 baseline 57.36 → ours 62.17)은 전혀 설명하지 못한다 — 저자들도
T.7/T.8 self-audit에서 이걸 스스로 인정함. ICLR/ICML급으로 이론을 보완하려면 "왜 되는지"를
설명하는 정리가 있어야 한다는 논의에서, 그 첫 시도로 Tsipras et al. 스타일의 최소 toy model을
만들어 "head를 얼리고 feature만 anchor하면 clean accuracy가 표준 AT보다 높아질 수 있다"는 걸
직접 증명해봄. 결과는 `theory_toy_cleangain.md`에 전체 유도 있음, 이 파일은 그 요약 + 상태.

## Toy model 요약

- $y\in\{\pm1\}$, 로버스트 feature $x_1=y$ (prob $p$), 벌크 feature $x_2,\dots,x_{d+1}\sim
  \mathcal N(\eta y,1)$ i.i.d. — 평균 $\bar x$는 clean엔 정보량 많지만($d\to\infty$ 시 $\eta y$로
  수렴) 공격($\ell_\infty$, budget $\varepsilon>\eta$)에는 취약(attacker가 부호까지 뒤집을 수 있음).
- **$d$** = 벌크 feature의 개수 (실제 네트워크의 feature 차원수 같은 것에 대응하는 토이 파라미터,
  clean/adversarial 선택과는 무관한 아키텍처 상수).
- **$\beta$** = 실제 학습 코드에 없는, toy model 안에서만 쓰는 mixing weight.
  $\phi_\beta = \beta\bar x+(1-\beta)\eta x_1$. $\beta=0$=표준 AT(robust하지만 clean 낮음),
  $\beta=1$=자연 teacher(clean 100%, robust 붕괴), $\beta=0.5$=둘을 섞은 지점.
- 세 가지 분류기 비교: teacher(clean=1,robust=0), 표준 AT(clean=robust=$p$), 우리 방법
  (head 고정 + $\phi_\beta$ 학습).

## 통과한 sanity check들 (전부 확인됨, scipy로 수치 검증까지 완료)

1. **$\beta\to0$**: $A_c\to p$, $A_r\to p$ — 표준 AT와 정확히 일치.
2. **$\beta=1$**: $A_c\to1$ (teacher clean과 일치), $A_r$은 $\varepsilon$에 따라 붕괴 (teacher와 일치).
3. **$d\to\infty$, $\beta=1/2$**: idealized(결정론적) 극한에서 손으로 푼 $A_c=(1+p)/2$,
   $A_r=p\cdot\mathbb1[\varepsilon<2\eta]$가 finite-$d$ 정확한 공식(가우시안 CDF $\Psi$ 사용)의
   $d\to\infty$ 극한과 정확히 일치.
4. **수치 검증** ($p=0.9,\eta=0.1,\varepsilon=0.12,\beta=0.5,d=2000$): 손 계산과 scipy
   `norm.cdf` 계산이 소수점까지 일치 — $A_c=0.950$, $A_r=0.8998$.
5. **$d$가 작거나 margin($2\eta-\varepsilon$)이 좁을 때 실패하는 것도 확인**: 같은 $\beta=0.5$인데
   $\varepsilon=0.15$(margin 0.05), $d=200$이면 $A_r$이 0.686으로 표준 AT(0.9)보다 오히려 나빠짐.
   즉 "공짜로 clean acc 오른다"는 주장이 무조건 성립하는 게 아니라 $(\eta,\varepsilon,d)$ 조건이
   딸린 진짜 정량적 주장이라는 걸 스스로 확인 — 이게 중요한 정직성 체크였음.

## 핵심 결과 (theory_toy_cleangain.md에 전체 유도)

$d=2000,\varepsilon=0.12$ (margin 0.08) 조건에서: **clean accuracy +5.0%p, robust accuracy
-0.02%p** — 거의 공짜인 Pareto improvement를 closed-form(가우시안 CDF)으로 증명. 가우시안이라
concentration inequality 없이 정확한 식이 나옴.

## 역산 검증 완료 (2026-08-25, item 1 & 4 해결)

$d$가 커야 한다는 조건은 부정확한 표현이었음 — 실제로 식을 정리하면 $d$와 $\beta$는 항상
$u=\beta/\sqrt d$ 결합으로만 등장해서, **$d$ 자체가 클 필요는 없고 $\beta/\sqrt d$가 작으면 됨**
(architecture의 실제 feature 차원과는 무관). "clean teacher라서 d가 크다"는 인과관계는
여전히 성립 안 하지만, 그게 애초에 문제가 아니었다는 게 밝혀짐.

**모델 확장**: 원래 모델은 $x_1$(로버스트 채널)이 공격에 전혀 안 흔들린다고 가정해서 표준
AT의 clean=robust=$p$가 되는데, 실제 baseline(ADR+WA+AWP, ResNet18/CIFAR100)은 clean 57.36%
vs robust(AA) 28.50% — **28.86%p 차이**. 그래서 $x_1$도 자기 노이즈($\sigma_1$)+공격예산
($\varepsilon_1$)을 갖도록 확장. 확장하니 $x_1$과 $\bar x$가 둘 다 가우시안이라 branch
나누기 없이 정확한 식이 나옴.

**역산 결과**: teacher clean 77.4%, baseline clean/robust(57.36/28.50) 세 숫자 + "두 채널에
노이즈 단위로 같은 세기의 공격이 걸린다"는 정규화 하나로 모델을 완전히 고정. 우리 방법의
실제 clean accuracy(62.34%)만 가지고 $\beta^*\approx0.151$을 맞춘 다음, **그 $\beta^*$에서
모델이 예측하는 robust accuracy(28.79%)를 실측값(28.44~28.59%)과 비교하니 0.2~0.35%p밖에
안 남**. clean 숫자 하나로 맞추고 robust 숫자를 예측한 거라 (둘 다 fit한 게 아님) 꽤 강한
검증. toy model이 실제 trade-off의 **크기(order)까지** 잘 잡아낸다는 뜻.

*단서*: "같은 세기의 공격" 정규화는 데이터에서 유도된 게 아니라 모델링 선택. 그래도 튜닝 없이
바로 0.3%p 이내로 맞았다는 게 포인트.

## $\beta$를 loss로 직접 최소화 — 3번 다시 계산해서 정확한 결론에 도달 (2026-08-25)

**1차**: "두 채널이 노이즈 단위로 똑같은 세기의 공격을 받는다"는 정규화(역산에 썼던 것) 그대로
쓰면 $\arg\min_\beta E[L]=1$ (teacher로 붕괴) — 근데 이 정규화가 $x_1$의 "덜 공격받는다"는
이점을 지워버린 상태라 별 의미 없음.

**2차**: 그 정규화를 풀고 $\bar x$ 채널의 공격 강도($\varepsilon\sqrt d$)를 거친 grid
(0.75,1,1.5,2,3,4...)로 스윕 → bang-bang처럼 보임 (거의 항상 $\beta\in\{0,1\}$). **이건 grid가
너무 거칠어서 생긴 착시였음.**

**3차 (정확한 계산)**: $E[L(\beta)]$는 $\beta$에 대해 **항상 convex인 진짜 이차식**(판별식이
항상 음수임을 대수적으로 확인). unconstrained vertex(제약 없는 진짜 최적점)가
$\varepsilon\sqrt d\approx1.8\to2.5$ 구간에서 $[0,1]$ box를 **연속적으로 관통**함 (2.0에서
0.93, 2.2에서 0.44). 그래서 $\varepsilon\sqrt d\approx2.32$인 지점에서 $\beta^*=0.151$이
**corner도 trajectory way-point도 아니라 진짜 global minimum 그 자체**임을 확인.

**하지만 완전히 안 닫힘**: 그 $\varepsilon\sqrt d=2.32$에서 teacher의 robust accuracy는
5.8%인데, 실제 자연학습 모델의 AutoAttack 성능은 보통 0%에 훨씬 가까움 — 그래서 좀 과대추정.
teacher robust를 더 realistic하게(1%, 0.1%, 0.01%) 잡으면 $\varepsilon\sqrt d$가 커지고
($3.08\to3.84\to4.47$), 그럴수록 $\beta=0.151$에서 예측되는 robust accuracy가 **오히려
더 나빠짐**(20.2%→16.7%→13.6%→11.3%, 실측 28.4~28.6%에서 더 멀어짐). 즉 이 모델은 자유
파라미터가 1개뿐이라, "clean 정확히 맞추기"와 "teacher robustness를 현실적으로 잡기"를
동시에 만족 못 함 — 3개 실측 숫자(teacher clean/ours clean/ours robust)를 "진짜 최적점"이라는
하나의 이야기로 한꺼번에 설명하기엔 이 2채널 linear model의 자유도가 부족함.

**최종 결론**: "frozen head + feature anchoring" 메커니즘 자체는 real이고, 아까 first-pass
curve-fit(β를 최적점이라 주장하지 않고 그냥 곡선 하나로 fit)이 보여준 clean-robust 교환비율은
실제 숫자와 order가 맞음. 하지만 "실제 학습의 동작 지점이 이 모델의 진짜 global optimum인지,
아니면 두 attractor 사이 trajectory 중간에 있는 건지"는 **이 reduced linear 모델로는
결론 못 냄** — 둘 다 어떤 파라미터 선택에서는 가능하지만 어느 쪽도 강제되지 않고, 두 개
이상의 실측 숫자를 "최적점" 얘기로 동시에 설명하려 하면 모델이 깨짐. 더 파고들려면 이
모델을 더 쥐어짜는 게 아니라 $\Phi_s$를 진짜 비선형/제약된 함수로 확장하거나 채널을
더 세분화해야 함.

## 최종 정리 — "왜 정확히 β≈0.15에서 멈추는지"는 애초에 이론이 답할 질문이 아니었음 (2026-08-25)

**사용자 지적으로 프레이밍을 다시 잡음**: "학습에는 LR도 있고 스케줄도 있는데, 그냥 거기서
수렴한 거 아니냐 — 왜 그 지점에 정확히 멈추는지를 toy model이 증명해야 하냐"는 지적이 맞았음.
"SGD/Adam이 정확히 그 좌표에서 멈추는 걸 이론이 증명해야 한다"는 건 애초에 과한 요구였음.

**실제 config로 확인**: `config/CIFAR100/champ_angeps_gnorm1.yaml` 등 실제 champion/featdir
레시피를 보면:
```
optim : AdamW
lr : 0.021                          # teacher의 SGD lr=0.1보다 훨씬 낮음
epochs : 100
finetune : True
finetune_checkpoint : .../clean_200ep/clean_last.pkl   # teacher 체크포인트에서 시작 (=toy model의 beta=1)
cyclic : True
```
student가 teacher 체크포인트에서 **정확히** 시작해서, teacher보다 훨씬 낮은 LR(AdamW,
0.021)로 100 epoch, OneCycle 스케줄로 finetuning. 이건 "population risk의 global optimum까지
갈 필요도 시간도 없는, 초기값에서 짧고 낮은 LR로 조금만 이동시키는" 세팅이 맞음 — 두 극단
(teacher / 표준 AT) 사이 어딘가에서 멈추는 게 오히려 이 레시피의 **자연스럽고 예상되는
동작**이라, toy model이 그 정확한 좌표를 "진짜 최적점"으로 증명하지 못한 건 결함이 아니었음.

**최종 결론 (확정)**:
1. 이론이 실제로 보여줘야 하는 것 = ① non-robust teacher를 앵커로 써도 robustness가 안 깎이는
   이유(T.2b, 여전히 유효) + ② clean accuracy가 오르면서 robustness는 안 깎이는 "공짜 구간"이
   실제로 존재하고, 그 교환비율의 크기(order)가 실측과 맞는다는 것(curve-fit, 0.2~0.35%p 오차
   — 이거 됨).
2. 이론이 보여줄 필요 없는 것 = SGD/AdamW가 정확히 어느 좌표에 수렴하는지 (이건 optimizer,
   LR, schedule, epoch 수, finetune 여부 같은 **레시피의 산물**이고, 실제로 확인한 champion
   레시피(AdamW lr=0.021, teacher-init finetune, 100ep)가 정확히 "짧고 낮은 LR로 조금만
   이동" 세팅이라 중간 지점에서 멈추는 게 당연함).

**이걸로 이 작업은 완료로 처리.** T.2b 보완용 toy model + 실측 정합성 검증 + "왜 정확한
좌표에 멈추는지는 이론 문제가 아니라 recipe 문제"라는 프레이밍까지 정리됨. 다음에 더
파고들 후보(낮은 우선순위): $\Phi_s$를 비선형/제약된 함수로 확장, 다채널 모델, 실제 AdamW
+ OneCycle 하에서의 SGD implicit bias 분석 — 지금은 안 함.

## 관련 파일
- `theory_toy_cleangain.md` — 전체 수식 유도, worked numeric instance, 3-pass 재계산 과정, open items 상세 (이 메모리 파일이 최종 결론이고 그 파일은 supporting math).
- `theory_v1.md` T.2b/T.2c — 이 toy model이 보완하려는 기존 이론 (robustness는 설명, clean acc는 미설명).
- `paper.md` §2 — T.2b의 논문 버전.
- `config/CIFAR100/champ_angeps_gnorm1.yaml` (및 형제 파일들) — 실제 champion 레시피 (AdamW lr=0.021, teacher-init finetune) 확인 소스.
