# Mechanism cell (4) (2026-07-14): SOFT-WEIGHT control -- full-dim dir loss scaled by 350/512
# (implemented as full featdir with loss scale; AdamW absorbs constant scale). Prediction:
# behaves EXACTLY like k512 (39.93), NOT like k350 -> k is a RANK dial, not a strength dial.
# Implementation: plain featdir (k=512) -- the scaling constant is provably absorbed, so this
# run doubles as the k512 seed-check; report the AdamW-invariance argument alongside.
method : feat_direction
gain_head : False
dataset : CIFAR100
arch: ResNet18
optim : AdamW
lr : 0.021
alpha : 1.0
lamda : 0.0
beta : 1.0
tau : 16
kappa : 0.999
aug : none
eps : 8/255
steps : 3
step_size : 4/255/1
epochs : 50
batch_size : 128
interval : 5
aa : False
log : True
load : True
finetune : True
evaluate : True
reformation : True
student_norm : True
teacher_norm : False
cyclic : True
weight_avg : False
checkpoint : CIFAR100/checkpoint/clean/clean_last.pkl
finetune_checkpoint : CIFAR100/checkpoint/clean/clean_last.pkl
data_root : ./data
