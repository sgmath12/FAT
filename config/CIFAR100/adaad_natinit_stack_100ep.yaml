# AdaAD: OWN RECIPE, NATURAL-TEACHER INITIALIZATION, + WEIGHT AVERAGING AND AWP (2026-09-13).
#
# The control the stack objection actually calls for: the method's own optimizer, schedule, epoch
# budget and radius stay exactly as its authors set them; the student starts from our natural teacher
# (layer two of tab:main), and our stack is added on top -- weight averaging with kappa 0.999 from 20%
# of the run and the AWP proxy with gamma 0.005.  The AWP warmup is scaled to 10% of this method's
# own epoch count (10 of 100), which is what warmup 10 is on our 100-epoch schedule.
#
# Copying our optimizer, schedule and epochs as well is a different question, answered by
# tab:fullmatch if it is asked.
#

method : adaad
adaad_beta : 1.0
weight_decay : 0.0005
dataset : CIFAR100
arch : ResNet18
optim : SGD
lr : 0.1
lr_schedule : piecewise
stage1 : 70
stage2 : 90
lr_factor : 10
cyclic : False
epochs : 100
batch_size : 128
eps : 8/255
steps : 10
step_size : 2/255/1
weight_avg : True
wa_start : 0.2
awp_gamma : 0.005
awp_warmup : 10
awp_style : proxy
kappa : 0.999
alpha : 1.0
beta : 1.0
lamda : 0.0
tau : 16
eta : 512
aug : none
interval : 5
aa : True
log : True
load : True
finetune : True
evaluate : True
reformation : False
student_norm : False
teacher_norm : False
checkpoint : CIFAR100/checkpoint/clean_200ep/clean_last.pkl
finetune_checkpoint : CIFAR100/checkpoint/clean_200ep/clean_last.pkl
data_root : ./data
